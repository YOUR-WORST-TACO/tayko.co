apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tayko-co.fullname" . }}-test-connection"
  labels:
{{ include "tayko-co.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tayko-co.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
