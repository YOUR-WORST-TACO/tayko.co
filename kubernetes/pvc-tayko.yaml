apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tayko-co-pvc
  labels:
    app: tayko-co
spec:
  storageClassName: do-block-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi