
@{
    ViewBag.Title = "title";
}

<h1>The person class</h1>
<p>Note, this is actually not a very good person class, I mean come on seriously... just look at it, even tim241
    commented on the crappiness of this porr excuse for a class</p>

<pre><code data-language="csharp">// The best person class ever written
public class Person
{
    public string FirstName { get; set; }
    public string Lastname { get; set; }
    public int Gender { get; set; }
    public DateTime Age { get; set; }

    public Person()
    {}

    public string GetName()
    {
        return $"{FirstName} {Lastname}";
    }
}
</code>
</pre>

<h1>The person class (but better)</h1>
<p>Taking the advice from many renowned experts on the field I have rebuilt my best person class ever written.
    This time it has it all, simpler code, no reduntant constructer, it even has lambdas.</p>

<pre><code data-language="csharp">// The best person class ever written
public class Person
{
    public string FirstName { get; set; }
    public string Lastname { get; set; }
    public int Gender { get; set; }
    public int Age { get; set; }
    public DateTime BirthDate { get; set; }

    public Person( string NewFirstName, string NewLastName, int NewGender)
    {
        FirstName = NewFirstName;
        LastName = NewLastName;

        Gender = NewGender;
        BirthDate = DateTime.Now;
        Age = 0;
    }

    public string GetName() => $"{FirstName} {Lastname}";
    
}
</code></pre>

@section Scripts
{
    <script>
    hljs.initHighlightingOnLoad();
    </script>
}